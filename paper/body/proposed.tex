\section{본 론}

사용자 $i$의 프로필 이미지를 $x_i$라 정의한다. 커플 쌍 $(x_f, x_m)$에 대해, 두 사용자의 임베딩 $e_f$, $e_m$이 임베딩 공간에서 가깝도록 학습하며, 비커플 쌍은 멀어지도록 대조 학습(Contrastive Learning)을 수행한다. 이를 위해 실제 데이팅 앱에서 상호 좋아요를 통해 매칭된 커플 775쌍의 프로필 이미지를 수집하였다. 각 커플 폴더에는 female.png와 male.png가 포함되어 있으며, 전체 데이터를 80/20 비율로 분할하여 표 \ref{tab:dataset}과 같이 Train+Valid(620쌍)와 Test(155쌍)로 구성하였다.

\begin{table}[h]
\centering
\caption{데이터셋 구성}
\label{tab:dataset}
\begin{tabular}{cc}
\toprule
\textbf{항목} & \textbf{값} \\
\midrule
총 커플 수 & 775쌍 \\
Train+Valid & 620쌍 (80\%) \\
Test (Hold-out) & 155쌍 (20\%) \\
\bottomrule
\end{tabular}
\end{table}

본 연구에서는 대규모 데이터로 사전 학습된 Qwen3-VL\cite{qwen2023}을 백본(Backbone)으로 사용한다. Qwen3-VL은 이미지와 텍스트를 함께 이해하는 비전-언어 모델(VLM)로, 다양한 시각적 태스크에서 뛰어난 일반화 성능을 보인다. 소규모 도메인 데이터에서의 과적합(Overfitting)을 방지하기 위해, 백본 네트워크의 약 20억 개 파라미터는 모두 동결(Freeze)한다. 그림 \ref{fig:architecture}는 제안하는 시스템의 전체 아키텍처를 나타낸다.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\columnwidth]{figures/system_architecture.png}
\caption{제안 시스템 아키텍처: Qwen3-VL 백본(동결)과 학습 가능한 프로젝션 헤드를 결합하여 커플 매칭 임베딩을 학습한다.}
\label{fig:architecture}
\end{figure}

백본을 통과한 고차원 특징 벡터 $h_i$는 식 (1)과 같다.

\begin{equation}
h_i = f_\theta(x_i) \in \mathbb{R}^{2048}
\end{equation}

매칭 호환성 학습에 특화된 저차원 임베딩을 얻기 위해, 학습 가능한 프로젝션 헤드(Projection Head) $g_\phi$를 추가한다. 프로젝션 헤드는 2개의 선형 계층(2048$\rightarrow$1024$\rightarrow$256)과 Layer Normalization, GELU 활성 함수로 구성된다.

\begin{equation}
z_i = g_\phi(h_i) = W_2 \cdot \text{GELU}(\text{LN}(W_1 \cdot h_i))
\end{equation}

\begin{equation}
e_i = \frac{z_i}{\|z_i\|_2} \in \mathbb{R}^{256}
\end{equation}

L2 정규화를 통해 모든 임베딩 벡터를 단위 초구(Unit Hypersphere) 상에 투영하여, 코사인 유사도와 유클리드 거리가 단조 관계를 가지게 한다.

실제 커플 쌍을 Positive, 배치 내 다른 사용자들을 Negative로 하여 대조 학습을 수행한다. CLIP\cite{radford2021}에서 검증된 InfoNCE Loss를 사용하며, 손실 함수는 식 (4)와 같다.

\begin{equation}
\mathcal{L} = -\log\frac{\exp(\text{sim}(e_f, e_m)/\tau)}{\sum_{j=1}^{N} \exp(\text{sim}(e_f, e_{m_j})/\tau)}
\end{equation}

여기서 $\tau$는 temperature 하이퍼파라미터이며, $\text{sim}$은 코사인 유사도, $N$은 배치 크기를 나타낸다. Temperature가 낮을수록 유사도 분포가 뾰족(sharp)해지고, 높을수록 부드러워진다. 본 연구에서는 실험을 통해 $\tau=0.1$이 최적임을 확인하였다. 또한 학습 효율을 높이기 위해 Semi-Hard Negative Mining을 적용하여, 너무 쉽거나 어렵지 않은 적절한 난이도의 네거티브 샘플을 선택함으로써 학습 안정성과 수렴 속도 간의 균형을 확보하였다.
